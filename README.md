# 🚀 Employee Directory Backend

This is the backend API for the **Employee Directory** application, built with **Node.js**, **Apollo Server 4**, **GraphQL**, and **MongoDB**. It exposes GraphQL endpoints for managing employee records including listing, filtering by department, and adding new employees.

---

## 📦 Tech Stack

- Node.js
- Apollo Server 4
- GraphQL
- MongoDB (via Mongoose)
- TypeScript (optional, or plain JS)

---

## Installation
 - clone the repository
 - add a file called .env at root and add a variable called `MONGO_URI=mongodb://localhost:27017/employee-db`
 - npm install
 - npm run dev

